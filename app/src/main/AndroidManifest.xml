<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SAMEGLOBAL_LOTO5_90"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GenerateurActivity"
            android:exported="false" />

        <activity
            android:name=".AfficherGrillesActivity"
            android:exported="false" />

        <activity
            android:name=".VerificationActivity"
            android:exported="false" />

        <activity
            android:name=".StatistiquesActivity"
            android:exported="false" />

        <activity
            android:name=".HistoriqueActivity"
            android:exported="false" />

        <activity
            android:name=".ModuloActivity"
            android:exported="false" />

    </application>

</manifest>
